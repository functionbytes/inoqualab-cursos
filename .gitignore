/vendor/
node_modules/
npm-debug.log
yarn-error.log

# Laravel 4 specific
bootstrap/compiled.php
app/storage/

# Laravel 5 & Lumen specific
public/storage
public/hot

# Laravel 5 & Lumen specific with changed public path
public_html/storage
public_html/hot

storage/*.key
.env
Homestead.yaml
Homestead.json
/.vagrant
.phpunit.result.cache

/public/build
/storage/pail
# Cualquier variante de .env queda fuera, no solo las tres que se listaban:
# apareció un `.env.bak-20260823-155226` con 73 variables (contraseñas de base
# de datos, Redis y SMTP, claves de Wompi) que ningún patrón cubría y que un
# `git add .` habría subido al repositorio.
.env.*
!.env.example
/storage/app/analytics
.phpactor.json
auth.json

# Editor / OS
/.idea
.idea/
.DS_Store

# Storage logs / worker
/storage/*.log
/storage/worker/

# Workflows de CI: excluidos del push por falta del scope "workflow" en el token.
# El ci.yml sigue en local; para subirlo, otorga el scope y quita esta línea.
/.github/workflows/
